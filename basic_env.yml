name: is
channels:
    - conda-forge
dependencies:
    - python=3.8
    - pip
    - numpy
    - scipy
    - pandas
    - scikit-learn
    - matplotlib
    - seaborn
    - jupyterlab
    - isort
    - flake8
    - black
    - mypy
    - nodejs # for jupyter lab widgets
    - ipywidgets # other extensions: jupyterlab-manager, jupyterlab_vim, jupyterlab-matplotlib
    - pynvim # for deoplete and other vim plugins

# Install pytorch separately

# Run `pip install requirements.txt` afterwards
